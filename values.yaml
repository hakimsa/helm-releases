image:
  repository: hakimsamouh/backend-mgt-app
  tag: dev
  pullPolicy: Always

replicaCount: 1

service:
  type: ClusterIP
  port: 8080

config:
  applicationProperties: |
    server.port=8080
    spring.jpa.hibernate.ddl-auto=update
    spring.jpa.show-sql=true

secret:
  dbUrl: jdbc:mysql://192.168.0.19:3306/dbs
  dbUser: "root"        # viene de GitHub Secret
  dbPassword: ""    # viene de GitHub Secret